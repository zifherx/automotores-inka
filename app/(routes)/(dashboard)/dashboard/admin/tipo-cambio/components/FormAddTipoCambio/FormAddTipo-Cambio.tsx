import { useState } from "react";

import { tFormAdding } from "@/types";

export function FormAddTipoCambio({ setOpenDialog }: tFormAdding) {
  const [isLoading, setIsLoading] = useState(false);

  return <div>FormAddTipo-Cambio</div>;
}
